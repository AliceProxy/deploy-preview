---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: quote-deploy-preview-pr10
  namespace: deploy-previews
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quote-deploy-preview-pr10
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: quote-deploy-preview-pr10
    spec:
      containers:
        - name: backend
          image: alicewasko/quote-previews:10-8fbcfa5
          ports:
            - name: http
              containerPort: 8080
          resources:
            limits:
              cpu: "0.1"
              memory: 100Mi
